@startuml
|User|
start
:Starts order process;
|User|
:Selects items;
:Displays selected item;
|User|->:Proceeds to checkout;
|User|->:Sends order request with item details;
|Client|
:Checks item availability;
if (Items available?) then (yes)
  |Client|->:Reserve items for order;
  |Client|->:Process payment;
  |Client|->:Store order details;
  |Client|->:Sends order confirmation;
else (no)
  |Client|->:Sends unavailability message;
endif
|User|
stop
@enduml
